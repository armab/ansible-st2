machine:
  services:
    - docker

test:
  override:
    -
      ? |
        case $CIRCLE_NODE_INDEX in
        0)
          bundle exec kitchen test ubuntu-14.04
        ;;
        1)
          bundle exec kitchen test ubuntu-12.04
        ;;
        esac
      :
        parallel: true
