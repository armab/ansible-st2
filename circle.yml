machine:
  services:
    - docker

test:
  override:
    -
      ? |
        case $CIRCLE_NODE_INDEX in
        0)
          bundle exec kitchen test ubuntu-14
        ;;
        1)
          bundle exec kitchen test ubuntu-12
        ;;
        esac
      :
        parallel: true
